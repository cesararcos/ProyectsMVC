@model IEnumerable<ProyectsMVC.DAL.Models.Priorities>

@{
    ViewBag.Title = "Priorities";
}

<h2>Priorities</h2>

<p>
    @Html.ActionLink(" New", "Create", null, new { @class = "btn btn-primary fas fa-plus-circle" })
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>
        <td>

            @Html.ActionLink(" ", "Edit", "Priorities", new { id = item.Id }, new { @class = "btn btn-primary btn-sm fas fa-edit" })
            @Html.ActionLink(" ", "Details", "Priorities", new { id = item.Id }, new { @class = "btn btn-primary btn-sm fas fa-info-circle" })
            @Html.ActionLink(" ", "Delete", "Priorities", new { id = item.Id }, new { @class = "btn btn-primary btn-sm fas fa-trash" })

            @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
        @Html.ActionLink("Details", "Details", new { id=item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
        </td>
    </tr>
}

</table>
